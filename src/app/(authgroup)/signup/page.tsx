import React from 'react'

const SignupPage = () => {
  return <div>SignupPage</div>
}

export default SignupPage

/**
 * (folderName) : the () makes it a route group, and this folder name is ommitted from the URL path
 * thus, we now access signup page at the url: `http://localhost:3000/signup` instead of `http://localhost:3000/authgroup/signup`
 */
